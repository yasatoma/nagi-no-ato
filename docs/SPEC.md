# 仕様書（ネタバレなし）

日本語オリジナル・ミステリービジュアルノベル。題名『凪のあとに、声が残る』。静的HTML/CSS/JavaScript、サーバー処理なし。PC横長、スマートフォン縦持ち。自作Web Audioによる音楽・雨・効果音。生成オリジナル美術。本文は別データ。

必須：タイトル、本編、条件付き選択、履歴、6手動保存＋自動保存、読了記録、設定、復活コード（UTF-8/Base64URL、版番号・整合性検証）、ファイル書出／読込、オート、既読スキップ、証拠帳、エンディング一覧、音量・文字速度・文字サイズ・演出低減。未読エンド名は隠す。恋愛値なし。

所要時間は文章量からの目安をREADMEに記載し、実測と区別。水増ししない。2026-09-06のユーザー指示によりGitHub Pagesで一般公開し、必要に応じてリポジトリをPublicにする。ネタバレ資料はdocs/spoilersに分離し、Pagesの配信対象から除外する。初回起動はタイトルのみ。音は開始操作以降。

最終仕様：背景6、透過人物5、全57場面・1,543段落・48,630字。読み込み前のバックアップ1枠。引き継ぎには連続ID圧縮とdeflate、チェック値を使用。QR生成および画像からのQR読取を同梱ライブラリでオフライン処理。カメラの常時使用は不要。
