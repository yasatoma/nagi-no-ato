# 検証記録（ネタバレなし）

2026-09-05、Windows環境。Node.js 24.12、インストール済みMicrosoft EdgeをPlaywrightで実際に起動して確認。ブラウザ保存領域はプレイヤー用と隔離。

## 結果

| 項目 | 結果 |
|---|---|
| 開始から全5結末まで | 実ブラウザで到達、表示確認 |
| 全場面・進行状態 | 57場面・5,729状態を探索、到達不能／行き止まりなし |
| 選択条件と再調査 | 不足条件で無効、再調査と証言聞き直しで回復を実操作確認 |
| セーブ・ロード | 6枠の保存構造、実操作の保存・復元・上書き確認 |
| localStorage | 再読み込み後も現在位置・設定が残る |
| 保存できない環境 | エラー表示とコードへの案内、本編は継続可能 |
| 引き継ぎ | 別のブラウザコンテキストに復元、破損コード拒否 |
| QR | 発行したQR画像をアップロードして読取、一致確認。全既読入りコードも独立したデコーダーで一致 |
| 既読スキップ | 未読で停止、選択肢で停止 |
| オート | 設定パネル表示中に進まない |
| 履歴・キーボード | 履歴への登録、Lキー、Escape、画面操作を確認 |
| 読了後 | 結末から直前の選択へ戻れる |
| 音 | AudioContextがrunning、出力波形が非ゼロであることを実測 |
| 美術 | 全11PNGが正常にデコード。人物5枚に透過アルファあり |
| PC | 1440×900、1366×768で確認 |
| 縦画面 | 390×844、375×667、320×568、768×1024で操作と横はみ出しを確認 |
| 読みやすさ | PC・縦画面スクリーンショットを目視確認、文字拡大30pxも確認 |
| 通信・JS | テスト中のJavaScript例外0、素材のHTTPエラー0 |
| 制作資料の分離 | docs、tests、.gitおよび符号化パスからのアクセスは404 |

## 実施コマンド

`npm test`、`npm run test:browser`、`node tests/interaction.mjs`。

画像・JSON結果は `test-results/`。終盤の内容が含まれるため初見では開かないでください。シナリオの真相・伏線対応監査は `docs/spoilers/AUDIT.md` に分離しています。

## 修正して再確認した事項

- 引き継ぎ欄のボタン生成で起きるDOM例外を修正。
- モノローグ位置でロードしたときにも、それまでに登場した人物を正しく復元。
- ロード前の記録を専用のバックアップ枠へ保持。
- 素材切替の完了後に画面を撮影して、実際の背景・人物合成を確認。
- 初回の調査順に依存して説明が抜けていた箇所を補正。

## 範囲

スマートフォン相当の画面・タッチを想定した寸法は検証済みですが、iPhone／Android実機での確認は未実施です。人が通常速度で読む所要時間は未実測。音声は出力波形と場面切替を検証しています。作品の読み味や音量の好みは初見プレイで調整できます。
